# 程式開發說明 - Gemini Anki TTS

## 專案簡介

本專案旨在簡化將文字轉換為語音（TTS）後，將音訊檔案匯入 Anki 的流程。程式透過監控指定資料夾中的音訊檔案，並提供簡單的命令列介面，讓使用者可以快速地將音訊移動到 Anki 的媒體資料夾、重新命名，並將對應的 Anki 音訊標籤複製到剪貼簿。

## 功能特色

  * **自動化流程**：一鍵完成檔案移動、重新命名和 Anki 標籤生成。
  * **簡易操作介面**：清晰的指令提示，方便使用者操作。
  * **高度可自訂性**：使用者可以自行設定 FFmpeg 路徑、Anki 媒體資料夾路徑以及目標音訊格式。
  * **格式轉換**：支援將原始音訊檔案轉換為 `mp3` 或 `wav` 格式。
  * **音訊播放速度調整**：可自訂音訊的播放速度，並透過 FFmpeg 進行處理。
  * **剪貼簿整合**：自動將 Anki 所需的 `[sound:...]` 標籤複製到剪貼簿，方便直接貼入卡片。

## 檔案結構

```
.
├── main.py          # 主程式進入點
├── config.json      # 設定檔
└── README.md        # 專案說明文件
```

## 開發環境

  * **語言**：Python 3.x
  * **外部函式庫**：
      * `pyperclip`：用於操作剪貼簿。
      * `shutil`：用於檔案操作。
      * `os`：用於路徑和檔案系統操作。
      * `subprocess`：用於執行外部命令 (FFmpeg)。
      * `json`：用於讀寫設定檔。
      * `datetime`：用於生成時間戳記。

## 核心邏輯說明

### 1\. 程式啟動

程式執行後，會顯示主選單：

```
--- Gemini Anki TTS ---
按 Enter 將檔案移動到Anki資料夾，'s' 進入設定，'c'複製 Prompt，'q' 退出。
```

程式會監聽使用者的鍵盤輸入，並根據不同的按鍵執行相應的功能。

### 2\. 設定 (`'s'`)

  * 當使用者按下 `s`，程式將進入設定模式。

  * 程式會讀取 `config.json` 檔案。如果檔案不存在，則會使用預設值創建一個新的。

  * **預設的 `config.json` 內容如下（已新增 `playback_speed`）：**

    ```json
    {
        "source_path": "D:\\下載.wav",
        "ffmpeg_path": "D:\\Code\\ffmpeg\\ffmpeg-master-latest-win64-gpl-shared\\bin\\ffmpeg.exe",
        "anki_media_path": "C:\\Users\\Admin\\AppData\\Roaming\\Anki2\\使用者 1\\collection.media",
        "audio_format": "wav",
        "playback_speed": 1.0
    }
    ```

  * 程式會依序提示使用者輸入或修改以下設定：

    1.  **來源檔案路徑 (source\_path)**：預設為 `D:\\下載.wav`。
    2.  **FFmpeg 執行檔路徑 (ffmpeg\_path)**：用於音訊格式轉換與變速。
    3.  **Anki 媒體資料夾路徑 (anki\_media\_path)**：存放 Anki 卡片音訊的目錄。
    4.  **音訊格式 (audio\_format)**：提示使用者輸入 `mp3` 或 `wav`。
    5.  **播放速度 (playback\_speed)**：提示使用者輸入音訊播放速度，預設為 `1.0` (原速)。

  * 輸入完成後，新的設定會被儲存回 `config.json` 檔案中。

### 3\. 執行主要功能 (`Enter`)

  * 當使用者按下 `Enter`，程式會執行核心的檔案處理流程。
  * **讀取設定**：首先讀取 `config.json` 的所有設定值，包含 `playback_speed`。
  * **生成檔名**：
      * 獲取當前的時間戳記 (例如 `YYYYMMDD_HHMMSS`)。
      * 根據設定的 `audio_format` 生成新的檔案名稱，格式為 `gemini-當前時間戳記.wav` 或 `gemini-當前時間戳記.mp3`。
  * **音訊處理與移動**：
      * **檢查是否需要調整播放速度**：讀取 `playback_speed` 的值。
      * **如果 `playback_speed` 不為 `1.0`**：
        1.  **無論格式是否相同，都必須使用 FFmpeg 進行處理**，因為需要重新編碼音訊來改變速度。
        2.  使用 `subprocess` 模組呼叫 FFmpeg，並透過 `-filter:a` 參數設定 `atempo` 濾鏡來調整速度。
        3.  命令範例 (例如，加速 1.2 倍)：`ffmpeg -i "D:\\下載.wav" -filter:a "atempo=1.2" "C:\\...\\collection.media\\gemini-20250812_174739.mp3"`
        4.  處理成功後，刪除原始的 `D:\\下載.wav` 檔案。
      * **如果 `playback_speed` 等於 `1.0` (原速)**：
          * **檢查 `audio_format` 是否與來源檔案副檔名不同**：
            1.  **若格式不同**：使用 `subprocess` 呼叫 FFmpeg 進行格式轉換 (不含變速濾鏡)。命令範例：`ffmpeg -i "D:\\下載.wav" "C:\\...\\collection.media\\gemini-20250812_174739.mp3"`。轉換成功後刪除原始檔。
            2.  **若格式相同**：使用 `shutil.move()` 直接將檔案移動到 `anki_media_path`，然後用 `os.rename()` 重新命名為 `gemini-當前時間戳記.格式`。
  * **複製 Anki 音訊標籤**：
      * 生成 Anki 音訊標籤字串，格式為 `[sound:新檔名]` (例如 `[sound:gemini-20250812_174739.mp3]`)。
      * 使用 `pyperclip.copy()` 將此字串複製到使用者的剪貼簿。
  * **完成提示**：在終端機顯示成功訊息，告知使用者檔案已處理且標籤已複製。

### 4\. 複製 Prompt (`'c'`)

  * 此功能為一個輔助選項，當使用者按下 `c` 時，程式會將預設的或特定的提示詞 (Prompt.txt) 複製到剪貼簿。

### 5\. 退出 (`'q'`)

  * 當使用者按下 `q`，程式將顯示退出訊息並終止執行。